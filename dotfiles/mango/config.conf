
# --- Main Workground binds
bind=SUPER,Return,spawn,kitty
bind=SUPER,b,spawn,vivaldi
bind=SUPER,q,killclient,
bind=SUPER,v,spawn,wofi --show drun
bind=SUPER+SHIFT,R,reload_config
bind=,Print,spawn,grim -g "$(slurp)" - | wl-copy
bind=NONE,Print,spawn_shell,grim $HOME/Pictures/Screenshots/$(date +%Y%m%d%H%M%S).png

# --- Scratchpads & Window rules ---
windowrule=appid:Spotify,isnamedscratchpad:1,width:2000,height:700
bind=SUPER,s,toggle_named_scratchpad,Spotify,scratch-spotify,spotify

windowrule=isnamedscratchpad:1,width:1000,height:700,appid:pulsemixer-scratch
bind=SUPER,p,toggle_named_scratchpad,pulsemixer-scratch,none,kitty --class pulsemixer-scratch pulsemixer

windowrule=appid:thunar,isfloating:1,width:1000,height:700,offsetx:0,offsety:0
windowrule=title:Picture in picture,isfloating:1,isglobal:1,isoverlay:1,width:480,height:270,offsetx:20,offsety:-20

windowrule=isnamedscratchpad:1,width:1000,height:700,appid:kitty
bind=SUPER,t,toggle_named_scratchpad,kitty,none,kitty


# --- Post Launch
exec-once=quickshell
exec-once= swaybg -i ~/.config/mango/assets/nord.jpg -m fill
exec-once = set-wallpaper ~/.config/mango/assets/nord.jpg
exec-once=lxsession
exec-once=dunst


# --- Display: 1080p, 165, positioned at origin
monitorrule=name:DP-2,width:1920,height:1080,refresh:165,x:0,y:0,scale:1,rr:0,vrr:0
borderpx=0

# --- Graphic Optimization & Wallpapers
blur_optimized=1
bind = SUPER,SHIFT,W, exec, set-wallpaper ~/.config/mango/assets/nord.jpg

# --- Focus movement (change which window is focused) ---
bind=SUPER,h,focusdir,left
bind=SUPER,l,focusdir,right
bind=SUPER,k,focusdir,up
bind=SUPER,j,focusdir,down

# --- Swap window with neighbor in direction ---
bind=SUPER+SHIFT,h,exchange_client,left
bind=SUPER+SHIFT,l,exchange_client,right
bind=SUPER+SHIFT,k,exchange_client,up
bind=SUPER+SHIFT,j,exchange_client,down

# --- Move floating windows (arrow-based, per docs' smartmovewin) ---
bind=SUPER+CTRL,h,smartmovewin,left
bind=SUPER+CTRL,l,smartmovewin,right
bind=SUPER+CTRL,k,smartmovewin,up
bind=SUPER+CTRL,j,smartmovewin,down

# --- Move floating window (mouse-based) ---
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_right,moveresize,curresize

# --- Tag switching (Mango's "workspaces") ---
bind=SUPER,1,view,1
bind=SUPER,2,view,2
bind=SUPER,3,view,3
bind=SUPER,4,view,4
bind=SUPER,5,view,5
bind=SUPER,6,view,6
bind=SUPER,7,view,7
bind=SUPER,8,view,8
bind=SUPER,9,view,9

# --- Move focused window to a tag ---
bind=SUPER+SHIFT,1,tag,1
bind=SUPER+SHIFT,2,tag,2
bind=SUPER+SHIFT,3,tag,3
bind=SUPER+SHIFT,4,tag,4
bind=SUPER+SHIFT,5,tag,5
bind=SUPER+SHIFT,6,tag,6
bind=SUPER+SHIFT,7,tag,7
bind=SUPER+SHIFT,8,tag,8
bind=SUPER+SHIFT,9,tag,9

# --- Cycle tags left/right ---
bind=SUPER,bracketleft,viewtoleft
bind=SUPER,bracketright,viewtoright

# --- Monitor focus ---
bind=SUPER+ALT,h,focusmon,left
bind=SUPER+ALT,l,focusmon,right
